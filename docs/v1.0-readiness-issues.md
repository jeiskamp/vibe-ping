# VibePing v1.0 Readiness Issues

This list turns the release audit into concrete work items we can close in order.

## P0 Release Blockers

- [ ] Package the desktop app into installable artifacts for macOS, Windows, and Linux.
  - [x] Add `electron-builder` packaging scripts and config for VibePing watcher.
  - [x] Add cross-platform package smoke checks in CI (`ubuntu`, `windows`, `macos`).
  - [x] Add tag-triggered draft GitHub release workflow that publishes artifacts and checksums.
- [ ] Sign and notarize distributed builds where required.
  - [x] Add hardened runtime entitlements and notarization hook for macOS release builds.
  - [x] Fail release workflow fast when required macOS/Windows signing secrets are missing.
  - [x] Add manual macOS notarization preflight workflow to verify secrets before tag release.
- [x] Make the watcher app load renderer assets from `dist` so packaged builds can be self-contained.
- [x] Fix workspace build output so `@vibeping/shared` emits to its own `dist` directory.
- [x] Add CI release gates for `pnpm typecheck`, `pnpm build`, and smoke validation of packaged output.

## P1 Reliability

- [x] Catch network failures during Discord/backend test delivery and show clear app feedback.
- [x] Catch network failures in Discord send helpers so backend requests fail gracefully.
- [x] Move webhook credential storage to a more secure OS-backed secret store.
- [ ] Harden handling for missing, deleted, or unreadable watched folders with explicit user guidance.
- [ ] Add debounce or replay protection tests around aggregate presence transitions.

## P1 Diagnostics

- [x] Show last scan time in the app.
- [x] Show the current effective presence state in the app.
- [x] Show how many watched folders currently need review.
- [ ] Add a direct path or action to inspect local logs from the app.

## P1 Docs

- [ ] Rewrite the README for end-user release installation, not only repo-based development.
- [ ] Document webhook setup with screenshots or a tighter step-by-step guide.
- [ ] Document close vs quit behavior and tray behavior clearly.
- [ ] Add a known limitations section for scan-based activity detection.

## P2 Quality

- [x] Add automated tests for presence transition behavior.
- [ ] Add automated tests for idle threshold behavior.
- [ ] Add automated tests for Discord payload formatting.
- [x] Add automated tests for duplicate-send prevention.
- [ ] Polish default copy, including the default username seed.

## Current Focus

1. Add signing/notarization for production-grade distributables.
2. Add core behavior tests around presence transitions and duplicate sends.
3. Improve watched-folder edge-case handling and user-facing diagnostics for unreadable paths.
